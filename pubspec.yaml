name: ms4bfcw
description: Professional Secure App.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Firebase Core & Auth
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  firebase_database: ^10.4.0
  firebase_storage: ^11.6.0
  # Notifications
  firebase_messaging: ^14.7.10
  flutter_local_notifications: ^16.3.0
  # Media & UI
  image_picker: ^1.0.7
  permission_handler: ^11.3.0
  record: ^5.1.0 # Ses kaydı için
  audioplayers: ^5.2.1 # Ses oynatma için
  intl: ^0.19.0 # Tarih formatı
  google_fonts: ^6.1.0 # Şık fontlar için

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/ # Logo vb. gerekirse buraya
